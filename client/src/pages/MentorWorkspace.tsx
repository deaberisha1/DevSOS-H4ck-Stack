/** Placeholder — built in a later step of the build order. */
export default function MentorWorkspace() {
  return (
    <main id="main" className="centered">
      <h1>MentorWorkspace</h1>
      <p>Not built yet.</p>
    </main>
  );
}
